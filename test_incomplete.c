#include <stdio.h>

int main() {
    int x = 10;
    if (x > 5) {
        printf("x is greater than 5\n");
        // 미완성 코드 - 중괄호가 닫히지 않음
        if (x > 8) {
            printf("x is greater than 8\n");
            // 여기서 중괄호가 닫히지 않음 